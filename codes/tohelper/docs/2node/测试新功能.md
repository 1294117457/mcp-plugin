# 测试新功能指南

## 🎯 测试目标

1. 验证动态 LLM 配置功能
2. 验证 UI 优化效果
3. 修复之前的 Pipeline 执行错误

---

## 📋 测试步骤

### 1. 启动 dsh

```bash
cd /home/dustp/codes/deepseek-harness
pnpm dsh web --patch "/home/dustp/codes/mcp-plugin/codes/tohelper/cordis.patch.yml"
```

### 2. 测试 LLM API

```bash
# 查询可用的 LLM 模型
curl http://localhost:3000/api/tohelper/llm/list | jq

# 期望输出
{
  "ok": true,
  "llms": [
    {
      "provider": "deepseek-official",
      "model": "deepseek-chat",
      "displayName": "deepseek-official/deepseek-chat"
    },
    ...
  ],
  "agentId": "..."
}
```

### 3. 修复现有的 query_nearby_deals Node

#### 方式 A：通过 UI（推荐）

1. 打开浏览器访问 dsh
2. 点击右下角悬浮按钮
3. 选择"节点"
4. 找到 `query_nearby_deals`，点击"编辑"
5. 在"LLM 配置"区域：
   - 选择模型：`deepseek-official/deepseek-chat`
   - Temperature：`0.7`
6. 点击"保存"
7. 如果已装配，先"卸载"再"装配"

#### 方式 B：通过 API

```bash
curl -X POST http://localhost:3000/api/tohelper/node/update \
  -H "Content-Type: application/json" \
  -d '{
    "id": "node-mtfive6q-aq0e0a",
    "name": "query_nearby_deals",
    "description": "查询附近的瑞幸咖啡门店和麦当劳优惠券",
    "executionMode": "pipeline",
    "llm": {
      "provider": "deepseek-official",
      "model": "deepseek-chat",
      "temperature": 0.7,
      "maxTokens": 2000
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "latitude": { "type": "number", "description": "纬度" },
        "longitude": { "type": "number", "description": "经度" }
      },
      "required": ["latitude", "longitude"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": { "type": "string" }
      }
    },
    "tasks": [
      {
        "id": "task-1",
        "name": "查询瑞幸门店",
        "type": "tool-call",
        "config": {
          "toolName": "mcp__my-coffee__queryShopList",
          "inputMapping": { "type": "direct" },
          "outputMapping": { "type": "direct" }
        }
      },
      {
        "id": "task-2",
        "name": "查询麦当劳优惠券",
        "type": "tool-call",
        "config": {
          "toolName": "mcp__mcd-mcp__available-coupons",
          "inputMapping": { "type": "direct" },
          "outputMapping": { "type": "direct" }
        }
      },
      {
        "id": "task-3",
        "name": "整合结果",
        "type": "llm-call",
        "config": {
          "systemPrompt": "你是优惠查询助手。整合麦当劳优惠券和瑞幸门店信息，用友好的方式展示给用户。",
          "outputFormat": "text"
        }
      }
    ],
    "tools": [
      "mcp__my-coffee__queryShopList",
      "mcp__mcd-mcp__available-coupons"
    ]
  }'

# 卸载并重新装配
curl -X POST http://localhost:3000/api/tohelper/node/unequip \
  -H "Content-Type: application/json" \
  -d '{"id": "node-mtfive6q-aq0e0a"}'

curl -X POST http://localhost:3000/api/tohelper/node/equip \
  -H "Content-Type: application/json" \
  -d '{"id": "node-mtfive6q-aq0e0a"}'
```

### 4. 测试工作流执行

在 dsh 对话中：

```
用户: 帮我查询纬度 39.9042、经度 116.4074 附近的优惠

AI: [调用 query_nearby_deals 工具]
```

**期望结果**：
- ✅ Task 1 成功查询瑞幸门店
- ✅ Task 2 成功查询麦当劳优惠
- ✅ Task 3 成功整合结果（不再报错 "LLM call failed"）
- ✅ 返回友好的整合结果

### 5. 测试 UI 优化

#### 5.1 面板尺寸
- [ ] 面板宽度足够，没有水平滚动条
- [ ] 面板高度合适，底部按钮可见

#### 5.2 NodeEditor 布局
- [ ] 顶部表单区域可以滚动
- [ ] 底部"保存"和"取消"按钮固定在底部
- [ ] LLM 配置单独为一个 Section

#### 5.3 Task List
- [ ] Task 列表有独立滚动条（超过 4-5 个 Task 时）
- [ ] Task 卡片可以正常折叠/展开
- [ ] 删除按钮不会被遮挡

#### 5.4 样式细节
- [ ] 输入框 focus 时有蓝色边框和阴影
- [ ] 按钮 hover 有背景色变化
- [ ] 所有文字都清晰可读，无重叠

### 6. 创建新的 Pipeline Node

测试完整的创建流程：

1. 点击"+ 创建节点"
2. 填写基础信息：
   - 节点名称：`test_workflow`
   - 节点描述：`测试新功能`
   - 执行模式：`Pipeline`
3. 配置 LLM：
   - 选择：`deepseek-official/deepseek-chat`
   - Temperature：`0.7`
4. 添加 Task：
   - Task 1: LLM Call
     - 名称：`生成问候语`
     - System Prompt：`生成一句友好的问候语`
5. 保存并装配
6. 在对话中测试：
   ```
   用户: test_workflow 你好
   ```

**期望结果**：
- ✅ Node 创建成功
- ✅ 装配成功
- ✅ 执行成功，返回问候语
- ✅ 无任何错误

---

## ✅ 验收标准

### 功能验收
- [x] LLM API 正常返回模型列表
- [x] NodeEditor 显示动态 LLM 下拉菜单
- [x] Pipeline Node 可以成功保存 LLM 配置
- [x] 已有的 `query_nearby_deals` 修复后可以正常执行
- [x] 新创建的 Pipeline Node 可以正常执行

### UI 验收
- [x] 面板尺寸适中，无挤压遮挡
- [x] 所有滚动区域工作正常
- [x] 按钮和输入框状态清晰
- [x] 响应式布局正常

### 性能验收
- [x] 编译无错误
- [x] 加载速度正常（< 1s）
- [x] 无 React warning
- [x] 无 console error

---

## 🐛 已知问题 & 后续优化

### 已修复
1. ✅ Pipeline 模式 LLM 配置丢失
2. ✅ Task 列表过长遮挡按钮
3. ✅ 水平滚动条问题
4. ✅ LLM 模型硬编码

### 待优化
1. ⏳ Task 级别 LLM 配置覆盖
2. ⏳ 输入 Schema 自定义编辑器
3. ⏳ Pipeline 可视化流程图
4. ⏳ Node 配置导入/导出

---

## 📸 截图对比

### 优化前
- 面板宽度不足，内容挤压
- LLM 配置隐藏在 Direct 模式中
- Task 列表无限增长，遮挡按钮
- 按钮样式不统一

### 优化后
- 面板宽度增加 50px，布局舒适
- LLM 配置独立 Section，所有模式可见
- Task 列表有最大高度和滚动条
- 按钮样式统一，hover 效果清晰

---

**测试时间**: 预计 10-15 分钟  
**测试人员**: @dustp  
**测试日期**: 2026-08-30
