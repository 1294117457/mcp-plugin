# Node-Task 系统 - Host 端设计文档

## 一、核心概念

### 1.1 设计原则

- **Node = 内聚的工作单元**：一个 Node 对应一个明确的业务职责
- **Task = Node 内部的实现细节**：用于 pipeline 模式的多阶段编排
- **不侵入 dsh**：完全基于 Cordis 插件机制和 dsh 公开 API
- **复用 Tool 基础设施**：Task 可以调用已装配的 Tool（builtin/MCP/skill）

### 1.2 执行模式

| 执行模式 | 适用场景 | Task 角色 | LLM 配置 |
|---------|---------|----------|---------|
| `direct` | 简单的单次 LLM 调用 | 无 Task 概念 | Node 级配置 |
| `pipeline` | 多阶段处理流程 | Task = stage | 每个 Task 独立配置 LLM |
| `subagent` | 复杂的自主决策 | Task = agent 自主选择的 tool call | Node 级配置（未来扩展） |

---

## 二、数据结构设计

### 2.1 Task 定义

```typescript
// src/types.ts

/**
 * Task 配置定义（存储在 Node 配置中）
 */
export interface TaskConfig {
  id: string                      // Task ID（在 Node 内唯一）
  name: string                    // Task 名称（用于 UI 显示）
  description?: string            // Task 描述
  
  // Task 类型
  type: 'llm-call' | 'tool-call' | 'transform' | 'conditional'
  
  // Task 配置（根据 type 不同而不同）
  config: TaskTypeConfig
  
  // LLM 配置（可选，优先级高于 Node 级配置）
  llm?: LLMConfig
}

/**
 * 不同 Task 类型的配置
 */
export type TaskTypeConfig = 
  | LLMCallTaskConfig
  | ToolCallTaskConfig
  | TransformTaskConfig
  | ConditionalTaskConfig

/**
 * LLM 调用 Task 配置
 */
export interface LLMCallTaskConfig {
  systemPrompt: string            // System prompt
  userPromptTemplate?: string     // 用户输入模板（支持变量替换）
  outputFormat?: 'text' | 'json'  // 输出格式
}

/**
 * Tool 调用 Task 配置
 */
export interface ToolCallTaskConfig {
  toolName: string                // Tool 名称（从已装配的 Tool 中选择）
  inputMapping?: InputMapping     // 输入映射规则
  outputMapping?: OutputMapping   // 输出映射规则
}

/**
 * 数据转换 Task 配置
 */
export interface TransformTaskConfig {
  script: string                  // JavaScript 转换脚本
  // 例如: "input.data.map(x => x.toUpperCase())"
}

/**
 * 条件分支 Task 配置（未来扩展）
 */
export interface ConditionalTaskConfig {
  condition: string               // 条件表达式
  trueBranch: string              // 条件为真时执行的 Task ID
  falseBranch: string             // 条件为假时执行的 Task ID
}

/**
 * 输入/输出映射规则
 */
export interface InputMapping {
  type: 'direct' | 'extract' | 'template'
  
  // type=extract: 从上一步输出中提取字段
  // 例如: { "filepath": "$.result.path" }
  extractPaths?: Record<string, string>
  
  // type=template: 使用模板生成
  // 例如: "分析文件：${filepath}"
  template?: string
}

export interface OutputMapping {
  type: 'direct' | 'extract' | 'wrap'
  
  // type=extract: 从 Tool 输出中提取字段
  extractPaths?: Record<string, string>
  
  // type=wrap: 包装成新对象
  // 例如: { "analysis": "${output}" }
  wrapTemplate?: Record<string, string>
}

/**
 * LLM 配置
 */
export interface LLMConfig {
  provider: string                // LLM 提供商（暂时用 dsh 默认）
  model: string                   // 模型名称
  temperature?: number            // 温度
  maxTokens?: number              // 最大 token 数
  // 未来扩展：支持自定义 LLM endpoint
}
```

### 2.2 Node 定义（扩展）

```typescript
// src/types.ts

/**
 * Node 配置定义（扩展现有结构）
 */
export interface NodeConfig {
  id: string
  name: string
  description: string
  
  // ===== 执行模式 =====
  executionMode: 'direct' | 'pipeline' | 'subagent'
  
  // ===== direct 模式字段 =====
  systemPrompt?: string           // 仅 direct 模式使用
  llm?: LLMConfig                 // 仅 direct 模式使用
  
  // ===== pipeline 模式字段 =====
  tasks?: TaskConfig[]            // Task 列表（仅 pipeline 模式）
  
  // ===== 通用字段 =====
  tools?: string[]                // 可用的 Tool 列表（供 Task 调用）
  inputSchema: Record<string, unknown>   // JSON Schema
  outputSchema: Record<string, unknown>  // JSON Schema
  
  // ===== 元数据 =====
  createdAt: string
  updatedAt?: string
}

/**
 * Node 配置文件（持久化）
 */
export interface NodeConfigFile {
  version: 1
  nodes: Record<string, NodeConfig>     // nodeId -> NodeConfig
  equipped: string[]                    // 已装配的 Node ID 列表
}
```

### 2.3 Task 执行上下文

```typescript
// src/host/task/types.ts

/**
 * Task 执行上下文（运行时）
 */
export interface TaskContext {
  // dsh 服务注入
  llm: any                        // ctx.llm
  tools: any                      // ctx.tools
  agent?: any                     // 当前 agent（可选）
  
  // Node 级配置
  nodeConfig: NodeConfig          // 所属 Node 的配置
  
  // Pipeline 状态
  pipelineState: PipelineState    // Pipeline 共享状态
  
  // 当前输入
  input: unknown                  // 当前 Task 的输入
}

/**
 * Pipeline 状态（Task 间共享）
 */
export interface PipelineState {
  // 所有 Task 的输出结果
  taskOutputs: Record<string, unknown>  // taskId -> output
  
  // 全局变量（可选）
  variables: Record<string, unknown>
  
  // 执行历史（调试用）
  executionLog: Array<{
    taskId: string
    startTime: number
    endTime: number
    success: boolean
    error?: string
  }>
}
```

---

## 三、代码结构设计

### 3.1 目录结构

```
src/host/
├── task/                           # Task 系统
│   ├── types.ts                   # Task 类型定义
│   ├── registry.ts                # Task 注册表（管理 Task 类型）
│   ├── executor.ts                # Task 执行器（执行单个 Task）
│   ├── builtin/                   # 内置 Task 类型
│   │   ├── llm-call.ts           # LLM 调用 Task
│   │   ├── tool-call.ts          # Tool 调用 Task
│   │   ├── transform.ts          # 数据转换 Task
│   │   └── index.ts              # 导出所有内置 Task
│   └── index.ts                   # Task 模块入口
│
├── node/                           # Node 系统（扩展）
│   ├── types.ts                   # Node 类型定义
│   ├── config.ts                  # Node 配置管理（已有，需扩展）
│   ├── executor.ts                # Node 执行器（已有，需重构）
│   ├── routes.ts                  # Node API 路由（已有）
│   └── index.ts                   # Node 模块入口（已有）
│
└── index.ts                        # 插件入口
```

### 3.2 核心模块设计

#### 3.2.1 Task Registry（任务注册表）

```typescript
// src/host/task/registry.ts

import type { TaskConfig, TaskContext } from './types.js'

/**
 * Task 执行器接口
 */
export interface TaskExecutor {
  type: string
  execute(config: TaskConfig, context: TaskContext): Promise<unknown>
}

/**
 * Task Registry（单例）
 */
class TaskRegistry {
  private executors = new Map<string, TaskExecutor>()
  
  /**
   * 注册 Task 类型
   */
  register(executor: TaskExecutor): void {
    if (this.executors.has(executor.type)) {
      throw new Error(`Task type "${executor.type}" already registered`)
    }
    this.executors.set(executor.type, executor)
    console.log(`[tohelper] Task type "${executor.type}" registered`)
  }
  
  /**
   * 获取 Task 执行器
   */
  getExecutor(type: string): TaskExecutor | undefined {
    return this.executors.get(type)
  }
  
  /**
   * 执行 Task
   */
  async execute(config: TaskConfig, context: TaskContext): Promise<unknown> {
    const executor = this.executors.get(config.type)
    if (!executor) {
      throw new Error(`Unknown task type: ${config.type}`)
    }
    
    // 记录执行日志
    const startTime = Date.now()
    context.pipelineState.executionLog.push({
      taskId: config.id,
      startTime,
      endTime: 0,
      success: false
    })
    
    try {
      const result = await executor.execute(config, context)
      
      // 更新日志
      const log = context.pipelineState.executionLog[context.pipelineState.executionLog.length - 1]
      log.endTime = Date.now()
      log.success = true
      
      // 保存输出
      context.pipelineState.taskOutputs[config.id] = result
      
      return result
    } catch (error: any) {
      // 更新日志
      const log = context.pipelineState.executionLog[context.pipelineState.executionLog.length - 1]
      log.endTime = Date.now()
      log.success = false
      log.error = error?.message ?? String(error)
      
      throw error
    }
  }
  
  /**
   * 列出所有已注册的 Task 类型
   */
  listTypes(): string[] {
    return Array.from(this.executors.keys())
  }
}

// 全局单例
export const taskRegistry = new TaskRegistry()
```

#### 3.2.2 内置 Task - LLM Call

```typescript
// src/host/task/builtin/llm-call.ts

import type { TaskExecutor } from '../registry.js'
import type { TaskConfig, TaskContext, LLMCallTaskConfig } from '../types.js'

export const llmCallExecutor: TaskExecutor = {
  type: 'llm-call',
  
  async execute(config: TaskConfig, context: TaskContext): Promise<unknown> {
    const taskConfig = config.config as LLMCallTaskConfig
    
    // 构造用户输入
    let userContent: string
    if (taskConfig.userPromptTemplate) {
      // 使用模板（支持变量替换）
      userContent = replaceVariables(taskConfig.userPromptTemplate, {
        input: context.input,
        state: context.pipelineState
      })
    } else {
      // 直接使用输入
      userContent = typeof context.input === 'string'
        ? context.input
        : JSON.stringify(context.input, null, 2)
    }
    
    // LLM 配置（Task 级 > Node 级 > 默认）
    const llmConfig = config.llm ?? context.nodeConfig.llm ?? getDefaultLLM(context)
    
    try {
      // 调用 dsh LLM
      const prepared = await context.llm.prepareCall(
        {
          provider: llmConfig.provider,
          model: llmConfig.model,
          temperature: llmConfig.temperature,
          maxTokens: llmConfig.maxTokens,
        },
        AbortSignal.timeout(120_000)
      )
      
      const { BlockAssembler } = await import('@deepseek-ai/dsh-llm')
      const assembler = new BlockAssembler()
      
      const stream = prepared.stream({
        provider: llmConfig.provider,
        model: llmConfig.model,
        system: taskConfig.systemPrompt,
        messages: [{
          role: 'user',
          content: [{ type: 'text', text: userContent }]
        }],
        temperature: llmConfig.temperature,
        maxTokens: llmConfig.maxTokens,
      })
      
      for await (const chunk of stream) {
        assembler.push(chunk)
      }
      
      const blocks = assembler.blocks()
      const textParts = blocks
        .filter((b: any) => b.type === 'text')
        .map((b: any) => b.text)
      
      const output = textParts.join('\n') || '(no output)'
      
      // 根据输出格式处理
      if (taskConfig.outputFormat === 'json') {
        try {
          return JSON.parse(output)
        } catch {
          throw new Error('LLM output is not valid JSON')
        }
      }
      
      return output
    } catch (error: any) {
      throw new Error(`LLM call failed: ${error?.message ?? String(error)}`)
    }
  }
}

// 变量替换辅助函数
function replaceVariables(template: string, data: any): string {
  return template.replace(/\$\{([^}]+)\}/g, (_, path) => {
    const value = getValueByPath(data, path)
    return value !== undefined ? String(value) : `\${${path}}`
  })
}

function getValueByPath(obj: any, path: string): any {
  const keys = path.split('.')
  let result = obj
  for (const key of keys) {
    if (result && typeof result === 'object') {
      result = result[key]
    } else {
      return undefined
    }
  }
  return result
}

function getDefaultLLM(context: TaskContext): any {
  // 从 dsh 获取默认 LLM 配置
  try {
    const sel = (context as any).agentDefaultModel?.currentSelection?.()
    if (sel) return { provider: sel.provider, model: sel.model }
  } catch {}
  return { provider: 'deepseek-official', model: 'deepseek-chat' }
}
```

#### 3.2.3 内置 Task - Tool Call

```typescript
// src/host/task/builtin/tool-call.ts

import type { TaskExecutor } from '../registry.js'
import type { TaskConfig, TaskContext, ToolCallTaskConfig } from '../types.js'

export const toolCallExecutor: TaskExecutor = {
  type: 'tool-call',
  
  async execute(config: TaskConfig, context: TaskContext): Promise<unknown> {
    const taskConfig = config.config as ToolCallTaskConfig
    
    // 检查 Tool 是否在允许列表中
    if (context.nodeConfig.tools && !context.nodeConfig.tools.includes(taskConfig.toolName)) {
      throw new Error(`Tool "${taskConfig.toolName}" not allowed in this Node`)
    }
    
    // 输入映射
    let toolInput: any
    if (taskConfig.inputMapping) {
      toolInput = mapInput(context.input, taskConfig.inputMapping)
    } else {
      toolInput = context.input
    }
    
    try {
      // 调用 dsh Tool
      const agent = context.agent
      const result = agent
        ? await context.tools.execute(taskConfig.toolName, toolInput, agent)
        : await context.tools.execute(taskConfig.toolName, toolInput)
      
      // 输出映射
      if (taskConfig.outputMapping) {
        return mapOutput(result, taskConfig.outputMapping)
      }
      
      return result
    } catch (error: any) {
      throw new Error(`Tool call failed: ${error?.message ?? String(error)}`)
    }
  }
}

// 输入映射辅助函数
function mapInput(input: unknown, mapping: any): any {
  if (mapping.type === 'direct') {
    return input
  }
  
  if (mapping.type === 'extract') {
    const result: any = {}
    for (const [key, path] of Object.entries(mapping.extractPaths || {})) {
      result[key] = extractByPath(input, path as string)
    }
    return result
  }
  
  if (mapping.type === 'template') {
    return replaceVariables(mapping.template || '', { input })
  }
  
  return input
}

// 输出映射辅助函数
function mapOutput(output: unknown, mapping: any): any {
  if (mapping.type === 'direct') {
    return output
  }
  
  if (mapping.type === 'extract') {
    const result: any = {}
    for (const [key, path] of Object.entries(mapping.extractPaths || {})) {
      result[key] = extractByPath(output, path as string)
    }
    return result
  }
  
  if (mapping.type === 'wrap') {
    const result: any = {}
    for (const [key, template] of Object.entries(mapping.wrapTemplate || {})) {
      result[key] = replaceVariables(template, { output })
    }
    return result
  }
  
  return output
}

// JSONPath 简化实现
function extractByPath(obj: any, path: string): any {
  if (path.startsWith('$.')) {
    path = path.slice(2)
  }
  const keys = path.split('.')
  let result = obj
  for (const key of keys) {
    if (result && typeof result === 'object') {
      result = result[key]
    } else {
      return undefined
    }
  }
  return result
}

function replaceVariables(template: string, data: any): string {
  return template.replace(/\$\{([^}]+)\}/g, (_, path) => {
    const value = extractByPath(data, path)
    return value !== undefined ? String(value) : `\${${path}}`
  })
}
```

#### 3.2.4 内置 Task - Transform

```typescript
// src/host/task/builtin/transform.ts

import type { TaskExecutor } from '../registry.js'
import type { TaskConfig, TaskContext, TransformTaskConfig } from '../types.js'

export const transformExecutor: TaskExecutor = {
  type: 'transform',
  
  async execute(config: TaskConfig, context: TaskContext): Promise<unknown> {
    const taskConfig = config.config as TransformTaskConfig
    
    try {
      // 创建安全的执行上下文
      const sandbox = {
        input: context.input,
        state: context.pipelineState.taskOutputs,
        JSON: JSON,
        Math: Math,
        Date: Date,
        // 不暴露危险的全局对象
      }
      
      // 执行转换脚本
      const func = new Function(...Object.keys(sandbox), `return (${taskConfig.script})`)
      const result = func(...Object.values(sandbox))
      
      return result
    } catch (error: any) {
      throw new Error(`Transform failed: ${error?.message ?? String(error)}`)
    }
  }
}
```

#### 3.2.5 Task 模块入口

```typescript
// src/host/task/index.ts

import { taskRegistry } from './registry.js'
import { llmCallExecutor } from './builtin/llm-call.js'
import { toolCallExecutor } from './builtin/tool-call.js'
import { transformExecutor } from './builtin/transform.js'

export { taskRegistry, type TaskExecutor } from './registry.js'
export type { TaskConfig, TaskContext, PipelineState } from './types.js'

/**
 * 注册所有内置 Task 类型
 */
export function registerBuiltinTasks(): void {
  taskRegistry.register(llmCallExecutor)
  taskRegistry.register(toolCallExecutor)
  taskRegistry.register(transformExecutor)
  
  console.log(`[tohelper] Registered ${taskRegistry.listTypes().length} builtin task types`)
}
```

#### 3.2.6 Node Executor（重构）

```typescript
// src/host/node/executor.ts（重构版）

import type { Context } from '@deepseek-ai/cordis'
import type { NodeConfig } from '../../types.js'
import { taskRegistry } from '../task/index.js'
import type { TaskContext, PipelineState } from '../task/types.js'

export interface NodeExecutor {
  run(node: NodeConfig, args: unknown, agent?: any): Promise<{ result: string }>
}

export function createNodeExecutor(ctx: Context): NodeExecutor {
  return {
    async run(node, args, agent) {
      // ===== 1. direct 模式：单次 LLM 调用 =====
      if (node.executionMode === 'direct') {
        return runDirectMode(ctx, node, args, agent)
      }
      
      // ===== 2. pipeline 模式：Task 编排 =====
      if (node.executionMode === 'pipeline') {
        return runPipelineMode(ctx, node, args, agent)
      }
      
      // ===== 3. subagent 模式：未来扩展 =====
      if (node.executionMode === 'subagent') {
        throw new Error('subagent mode not implemented yet')
      }
      
      throw new Error(`Unknown execution mode: ${node.executionMode}`)
    }
  }
}

/**
 * Direct 模式执行（兼容旧版）
 */
async function runDirectMode(
  ctx: Context,
  node: NodeConfig,
  args: unknown,
  agent?: any
): Promise<{ result: string }> {
  const userContent = typeof args === 'string'
    ? args
    : JSON.stringify(args, null, 2)
  
  const llmConfig = node.llm ?? getDefaultLLMConfig(ctx)
  
  try {
    const prepared = await (ctx as any).llm.prepareCall(
      {
        provider: llmConfig.provider,
        model: llmConfig.model,
        temperature: llmConfig.temperature,
        maxTokens: llmConfig.maxTokens,
      },
      AbortSignal.timeout(120_000)
    )
    
    const { BlockAssembler } = await import('@deepseek-ai/dsh-llm')
    const assembler = new BlockAssembler()
    
    const stream = prepared.stream({
      provider: llmConfig.provider,
      model: llmConfig.model,
      system: node.systemPrompt || '',
      messages: [{ role: 'user', content: [{ type: 'text', text: userContent }] }],
      temperature: llmConfig.temperature,
      maxTokens: llmConfig.maxTokens,
    })
    
    for await (const chunk of stream) {
      assembler.push(chunk)
    }
    
    const blocks = assembler.blocks()
    const textParts = blocks
      .filter((b: any) => b.type === 'text')
      .map((b: any) => b.text)
    const result = textParts.join('\n') || '(no output)'
    
    return { result }
  } catch (e: any) {
    return { result: `[Node execution error] ${e?.message ?? String(e)}` }
  }
}

/**
 * Pipeline 模式执行（Task 编排）
 */
async function runPipelineMode(
  ctx: Context,
  node: NodeConfig,
  args: unknown,
  agent?: any
): Promise<{ result: string }> {
  if (!node.tasks || node.tasks.length === 0) {
    throw new Error('Pipeline mode requires at least one task')
  }
  
  // 初始化 Pipeline 状态
  const pipelineState: PipelineState = {
    taskOutputs: {},
    variables: {},
    executionLog: []
  }
  
  let currentInput = args
  
  try {
    // 串行执行所有 Task
    for (const taskConfig of node.tasks) {
      const taskContext: TaskContext = {
        llm: (ctx as any).llm,
        tools: (ctx as any).tools,
        agent,
        nodeConfig: node,
        pipelineState,
        input: currentInput
      }
      
      // 执行 Task
      const output = await taskRegistry.execute(taskConfig, taskContext)
      
      // 传递给下一个 Task
      currentInput = output
    }
    
    // 格式化最终输出
    const result = typeof currentInput === 'string'
      ? currentInput
      : JSON.stringify(currentInput, null, 2)
    
    return { result }
  } catch (e: any) {
    // 返回详细的错误信息（包含执行日志）
    const errorLog = pipelineState.executionLog
      .map(log => `  ${log.taskId}: ${log.success ? 'OK' : 'FAILED'} (${log.endTime - log.startTime}ms)${log.error ? ` - ${log.error}` : ''}`)
      .join('\n')
    
    return {
      result: `[Pipeline execution error] ${e?.message ?? String(e)}\n\nExecution log:\n${errorLog}`
    }
  }
}

function getDefaultLLMConfig(ctx: Context): any {
  try {
    const sel = (ctx as any).agentDefaultModel?.currentSelection?.()
    if (sel) return { provider: sel.provider, model: sel.model }
  } catch {}
  return { provider: 'deepseek-official', model: 'deepseek-chat' }
}
```

---

## 四、API 路由设计

### 4.1 Node API（扩展）

保持现有的 Node CRUD API，扩展支持 Task 配置：

```typescript
// src/host/node/routes.ts（扩展）

// POST /api/tohelper/node/create
{
  name: "code_analyzer",
  description: "Analyze code for security issues",
  executionMode: "pipeline",
  tasks: [
    {
      id: "read_file",
      name: "Read File",
      type: "tool-call",
      config: {
        toolName: "read_file",
        inputMapping: {
          type: "extract",
          extractPaths: { "path": "$.filepath" }
        }
      }
    },
    {
      id: "analyze",
      name: "Analyze Code",
      type: "llm-call",
      config: {
        systemPrompt: "You are a security expert...",
        userPromptTemplate: "Analyze this code:\n\n${input}"
      },
      llm: {
        provider: "deepseek-official",
        model: "deepseek-chat",
        temperature: 0.3
      }
    }
  ],
  tools: ["read_file", "grep"],
  inputSchema: {
    type: "object",
    properties: {
      filepath: { type: "string" }
    }
  },
  outputSchema: {
    type: "object",
    properties: {
      result: { type: "string" }
    }
  }
}
```

### 4.2 Task API（新增）

```typescript
// src/host/task/routes.ts（新增）

// GET /api/tohelper/task/types
// 返回所有已注册的 Task 类型
{
  ok: true,
  types: ["llm-call", "tool-call", "transform"]
}

// GET /api/tohelper/task/schema/:type
// 返回指定 Task 类型的配置 schema
{
  ok: true,
  type: "tool-call",
  schema: {
    type: "object",
    properties: {
      toolName: { type: "string" },
      inputMapping: { ... },
      outputMapping: { ... }
    }
  }
}
```

---

## 五、初始化流程

```typescript
// src/host/index.ts（修改）

import type { Context } from '@deepseek-ai/cordis'
import { createAgentTracker } from './agent-tracker.js'
import { setupToolModule } from './tool/index.js'
import { setupNodeModule } from './node/index.js'
import { registerBuiltinTasks } from './task/index.js'

export const name = 'tohelper'
export const inject = ['webServer', 'tools', 'llm'] as const

export function apply(ctx: Context): void {
  console.log('[tohelper] plugin loaded')
  
  // 1. 注册内置 Task 类型
  registerBuiltinTasks()
  
  // 2. 初始化 Agent Tracker
  const tracker = createAgentTracker(ctx)
  
  // 3. 初始化 Tool 模块
  setupToolModule(ctx, tracker)
  
  // 4. 初始化 Node 模块
  setupNodeModule(ctx, tracker)
  
  console.log('[tohelper] all modules initialized')
}
```

---

## 六、配置文件示例

### 6.1 Simple Node (direct 模式)

```json
{
  "id": "node-simple-001",
  "name": "text_translator",
  "description": "Translate text to another language",
  "executionMode": "direct",
  "systemPrompt": "You are a professional translator. Translate the user's input to English.",
  "llm": {
    "provider": "deepseek-official",
    "model": "deepseek-chat",
    "temperature": 0.3
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "result": { "type": "string" }
    }
  },
  "createdAt": "2026-08-30T07:00:00Z"
}
```

### 6.2 Complex Node (pipeline 模式)

```json
{
  "id": "node-complex-001",
  "name": "code_analyzer",
  "description": "Analyze code file for security issues",
  "executionMode": "pipeline",
  "tasks": [
    {
      "id": "read_file",
      "name": "Read File",
      "type": "tool-call",
      "config": {
        "toolName": "read_file",
        "inputMapping": {
          "type": "extract",
          "extractPaths": { "path": "$.filepath" }
        },
        "outputMapping": {
          "type": "wrap",
          "wrapTemplate": {
            "filepath": "${input.filepath}",
            "content": "${output}"
          }
        }
      }
    },
    {
      "id": "analyze",
      "name": "Security Analysis",
      "type": "llm-call",
      "config": {
        "systemPrompt": "You are a security expert. Analyze code for vulnerabilities and return JSON format.",
        "userPromptTemplate": "File: ${input.filepath}\n\nCode:\n${input.content}\n\nAnalyze security issues.",
        "outputFormat": "json"
      },
      "llm": {
        "provider": "deepseek-official",
        "model": "deepseek-chat",
        "temperature": 0.2
      }
    },
    {
      "id": "format_report",
      "name": "Format Report",
      "type": "transform",
      "config": {
        "script": "{ filepath: input.filepath, issues: input.issues || [], summary: `Found ${input.issues?.length || 0} issues` }"
      }
    }
  ],
  "tools": ["read_file", "grep"],
  "inputSchema": {
    "type": "object",
    "properties": {
      "filepath": { "type": "string", "description": "Path to the code file" }
    },
    "required": ["filepath"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "result": { "type": "string" }
    }
  },
  "createdAt": "2026-08-30T07:00:00Z"
}
```

---

## 七、实现优先级

### Phase 1: MVP（1-2 周）

- [x] Task 类型系统（Registry）
- [x] 内置 Task: llm-call, tool-call, transform
- [x] Node Executor 重构（支持 pipeline）
- [x] 基础数据结构和配置管理

### Phase 2: UI 集成（1 周）

- [ ] Client UI 实现（见 client 设计文档）
- [ ] Task 编排可视化
- [ ] LLM 选择器

### Phase 3: 高级特性（未来）

- [ ] Conditional Task
- [ ] Parallel Task 执行
- [ ] Subagent 模式
- [ ] Task 调试和日志可视化

---

## 八、注意事项

### 8.1 安全性

- Transform Task 使用 Function 构造函数，需要限制可访问的全局对象
- Tool Call Task 需要验证 Tool 是否在允许列表中
- 未来考虑：沙箱执行环境

### 8.2 性能

- Pipeline 执行是串行的，未来可考虑支持并行执行
- LLM 调用有超时机制（120s）
- Task 执行日志用于调试，生产环境可考虑精简

### 8.3 兼容性

- 保持对现有 direct 模式 Node 的向后兼容
- Node 配置文件版本号用于未来迁移

---

## 九、测试用例

### 9.1 单元测试

```typescript
// 测试 Task Registry
test('taskRegistry registers and executes task', async () => {
  const mockExecutor = {
    type: 'mock',
    execute: async (config, context) => 'mock result'
  }
  taskRegistry.register(mockExecutor)
  
  const result = await taskRegistry.execute(
    { id: 't1', type: 'mock', config: {} },
    { /* context */ }
  )
  
  expect(result).toBe('mock result')
})

// 测试 LLM Call Task
test('llm-call task executes with template', async () => {
  const config = {
    id: 't1',
    type: 'llm-call',
    config: {
      systemPrompt: 'You are a translator',
      userPromptTemplate: 'Translate: ${input.text}'
    }
  }
  
  const context = {
    input: { text: 'Hello' },
    llm: mockLLM,
    // ...
  }
  
  const result = await llmCallExecutor.execute(config, context)
  expect(result).toContain('translation')
})
```

### 9.2 集成测试

```typescript
// 测试 Pipeline 执行
test('node executor runs pipeline', async () => {
  const node = {
    executionMode: 'pipeline',
    tasks: [
      { id: 't1', type: 'tool-call', config: { toolName: 'read_file' } },
      { id: 't2', type: 'llm-call', config: { systemPrompt: 'Analyze' } }
    ]
  }
  
  const executor = createNodeExecutor(mockContext)
  const result = await executor.run(node, { filepath: 'test.js' })
  
  expect(result.result).toBeDefined()
})
```

---

## 十、文档和注释规范

- 所有公开接口添加 JSDoc 注释
- 复杂逻辑添加行内注释
- 配置文件提供 JSON Schema 定义
- API 文档使用 OpenAPI 规范

---

**文档版本**: v1.0  
**最后更新**: 2026-08-30  
**作者**: tohelper team
